var gulp = require('gulp-help')(require('gulp'));

gulp.task('test', 'Runs karma and lints code.', function () {
  console.log('eslint + karma todo');
});

gulp.task('build', 'Builds project (concat, ngmin, uglify).', function () {
  console.log('build todo');
});

gulp.task('dev', 'Launch dev environment for project.', function () {
  console.log('dev todo');
});

gulp.task('changelog', 'Launch dev environment for project.', function () {
  console.log('dev todo');
});
